# E-Kanban 开发任务：1.2 创建 E-Kanban 仓储接口和实现

## 任务说明
按照 E-Kanban 开发计划，第一批次第二个任务：为刚才创建的 6 个实体创建对应的仓储接口和实现。

## 前置条件
- ✅ 任务 1.1 已完成：6 个实体已创建在 `VOL.Entity/DomainModels/`

## 开发规范
$(cat docs/volcore-development-guide.md)

## 位置
- 接口放在 `EKanban/IRepositories/`
- 实现放在 `EKanban/Repositories/`

## 需要创建的仓储列表

| 实体 | 接口文件 | 实现文件 |
|------|----------|----------|
| BoardWorkItem | `IBoardWorkItemRepository.cs` | `BoardWorkItemRepository.cs` |
| ExecutionCard | `IExecutionCardRepository.cs` | `ExecutionCardRepository.cs` |
| ExecutionTask | `IExecutionTaskRepository.cs` | `ExecutionTaskRepository.cs` |
| ExecutionRun | `IExecutionRunRepository.cs` | `ExecutionRunRepository.cs` |
| Spec | `ISpecRepository.cs` | `SpecRepository.cs` |
| SpecEvaluation | `ISpecEvaluationRepository.cs` | `SpecEvaluationRepository.cs` |

## 接口要求
- 继承 `IBaseRepository<TEntity>`
- 对于 ExecutionCard，添加自定义方法：
  ```csharp
  Task<Dictionary<ExecutionCardStatus, List<ExecutionCard>>> GetCardsGroupedByStatusAsync();
  Task<List<ExecutionCard>> GetExpiredInProgressTasksAsync(TimeSpan timeout);
  ```

## 代码风格要求
- 接口命名：`IXXXRepository`
- 实现命名：`XXXRepository`
- 继承 `BaseRepository<T>`
- 遵循 VOL.Core 现有代码风格
- 构造函数注入 `ISqlSugarClient`
- 实现自定义接口方法

## 输出要求
1. 在 `EKanban/IRepositories/` 创建 6 个接口文件
2. 在 `EKanban/Repositories/` 创建 6 个实现文件
3. 确保语法正确，可以编译通过
4. 在 `implementation-plan.md` 中更新此任务状态为 ✅ 完成

## 检查清单
- [ ] 所有接口文件已创建在 `EKanban/IRepositories/`
- [ ] 所有实现文件已创建在 `EKanban/Repositories/`
- [ ] 正确继承基类接口和基类实现
- [ ] ExecutionCard 添加了自定义查询方法
- [ ] 依赖注入正确
- [ ] 语法正确，可编译
