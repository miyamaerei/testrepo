# 看板前后端对应关系检查后端实现方案

## 文档信息
- **版本**：1.0.0
- **作者**：系统开发团队
- **更新时间**：2026-04-07
- **状态**：初稿

## 实现目标

本方案旨在实现后端缺失的 API 接口，确保前端 `/kanban/index` 页面的所有功能都能与后端 API 接口正确对应。主要包括：

- 在 `ExecutionCardController` 中添加 `GetDetail` 方法，实现与前端调用的对应
- 确保 API 接口返回的数据结构与前端期望的结构一致
- 验证所有前端调用的 API 接口都已在后端实现
- 解决字段大小写问题，确保前后端数据交互正常
- 遵循三层结构，复用请求参数和返回参数

## 技术实现

### 1. 三层结构实现

#### 1.1 数据访问层（Repositories）

- **IExecutionCardRepository**：定义卡片数据访问方法
- **ITaskPhaseProgressRepository**：定义阶段进度数据访问方法
- **ITaskFileChangeRepository**：定义文件变更数据访问方法
- **IProjectRepository**：定义项目仓库数据访问方法

#### 1.2 服务层（Services）

- **IExecutionCardService**：定义卡片业务逻辑方法
- **添加 GetDetail 方法**：在服务层添加获取卡片详情的方法，包含阶段进度和文件变更

#### 1.3 控制器层（Controllers）

- **ExecutionCardController**：处理卡片相关的 HTTP 请求
- **添加 GetDetail 方法**：实现获取卡片详情的 API 接口

### 2. 复用请求参数和返回参数

#### 2.1 请求参数复用

- 利用现有的 `ManualCardCreateRequest` 和 `ManualCardUpdateRequest` 等请求参数类
- 为 `GetDetail` 方法添加适当的请求参数类

#### 2.2 返回参数复用

- 使用现有的 `WebResponseContent` 类作为统一的返回格式
- 创建卡片详情返回结构类 `ExecutionCardDetailResponse`，包含卡片基本信息、阶段进度和文件变更

### 3. 字段大小写处理

- **后端实体**：使用 PascalCase（驼峰命名法，首字母大写）
- **前端期望**：使用 camelCase（驼峰命名法，首字母小写）
- **解决方案**：在 API 响应中使用 JSON 序列化配置，自动将 PascalCase 转换为 camelCase

### 4. 添加 GetDetail 方法

#### 4.1 服务层实现

在 `IExecutionCardService` 接口中添加 `GetDetailAsync` 方法：

```csharp
/// <summary>
/// 获取卡片详情（包含阶段进度和文件变更）
/// </summary>
/// <param name="id">卡片ID</param>
/// <returns>卡片详情</returns>
Task<ExecutionCardDetailResponse> GetDetailAsync(int id);
```

在 `ExecutionCardService` 中实现该方法：

```csharp
public async Task<ExecutionCardDetailResponse> GetDetailAsync(int id)
{
    var card = await ((IExecutionCardRepository)repository).FindFirstAsync(c => c.Id == id);
    if (card == null)
    {
        throw new ArgumentException($"Card {id} not found");
    }

    // 获取阶段进度
    var phaseProgressList = await _phaseProgressRepository.FindAsync(p => p.ExecutionCardId == id);
    
    // 获取文件变更
    var fileChangeList = await _fileChangeRepository.FindAsync(f => f.ExecutionCardId == id);
    
    // 获取关联的项目信息
    ProjectRepositories projectRepository = null;
    if (card.ProjectRepositoryId.HasValue)
    {
        projectRepository = await _projectRepository.FindFirstAsync(p => p.Id == card.ProjectRepositoryId.Value);
    }

    // 构建返回数据结构
    var response = new ExecutionCardDetailResponse
    {
        Id = card.Id,
        Title = card.Title,
        Description = card.Description,
        Status = (int)card.Status,
        ExecutorType = (int)card.ExecutorType,
        BoardWorkItemId = card.BoardWorkItemId,
        BoardId = card.BoardId,
        ProjectRepositoryId = card.ProjectRepositoryId,
        SpecId = card.SpecId,
        FailureCount = card.FailureCount,
        NeedsManualIntervention = card.NeedsManualIntervention,
        InProgressStartedAt = card.InProgressStartTime,
        CreatedAt = card.CreatedAt,
        UpdatedAt = card.LastUpdated,
        IsManualCreated = card.IsManualCreated,
        PhaseProgressList = phaseProgressList.Select(p => new PhaseProgressResponse
        {
            Id = p.Id,
            ExecutionCardId = p.ExecutionCardId,
            Phase = (int)p.Phase,
            Status = (int)p.Status,
            StartedAt = p.StartedAt,
            CompletedAt = p.CompletedAt,
            PhaseLog = p.Logs
        }).ToList(),
        FileChangeList = fileChangeList.Select(f => new FileChangeResponse
        {
            Id = f.Id,
            ExecutionCardId = f.ExecutionCardId,
            FilePath = f.FilePath,
            ChangeType = (int)f.ChangeType,
            CommitHash = f.CommitHash,
            ChangedAt = f.ChangedAt
        }).ToList(),
        ProjectRepository = projectRepository != null ? new ProjectRepositoryResponse
        {
            Id = projectRepository.Id,
            Name = projectRepository.Name,
            LocalWorkingDir = projectRepository.LocalWorkingDir,
            GitRemoteUrl = projectRepository.GitRemoteUrl,
            DefaultBranch = projectRepository.DefaultBranch,
            Description = projectRepository.Description
        } : null
    };

    return response;
}
```

#### 4.2 控制器层实现

在 `ExecutionCardController` 中添加 `GetDetail` 方法：

```csharp
/// <summary>
/// 获取卡片详情（包含阶段进度和文件变更）
/// </summary>
[HttpGet]
public async Task<IActionResult> GetDetail(int id)
{
    try
    {
        var detail = await _service.GetDetailAsync(id);
        return Ok(VOL.Core.Utilities.WebResponseContent.Instance.OK("获取卡片详情成功", detail));
    }
    catch (ArgumentException ex)
    {
        return Ok(VOL.Core.Utilities.WebResponseContent.Instance.Error(ex.Message));
    }
    catch (Exception ex)
    {
        return Ok(VOL.Core.Utilities.WebResponseContent.Instance.Error("获取卡片详情失败: " + ex.Message));
    }
}
```

#### 4.3 添加依赖注入

在 `ExecutionCardController` 的构造函数中添加对 `ITaskPhaseProgressRepository`、`ITaskFileChangeRepository` 和 `IProjectRepository` 的依赖注入：

```csharp
private readonly IExecutionCardService _service;
private readonly IExecutionCardRepository _repository;
private readonly ITaskPhaseProgressRepository _phaseProgressRepository;
private readonly ITaskFileChangeRepository _fileChangeRepository;
private readonly IProjectRepository _projectRepository;

public ExecutionCardController(
    IExecutionCardService service,
    IExecutionCardRepository repository,
    ITaskPhaseProgressRepository phaseProgressRepository,
    ITaskFileChangeRepository fileChangeRepository,
    IProjectRepository projectRepository)
{
    _service = service;
    _repository = repository;
    _phaseProgressRepository = phaseProgressRepository;
    _fileChangeRepository = fileChangeRepository;
    _projectRepository = projectRepository;
}
```

### 5. 数据结构定义

#### 5.1 返回结构类

```csharp
/// <summary>
/// 卡片详情响应结构
/// </summary>
public class ExecutionCardDetailResponse
{
    public int Id { get; set; }
    public string Title { get; set; }
    public string Description { get; set; }
    public int Status { get; set; }
    public int ExecutorType { get; set; }
    public int BoardWorkItemId { get; set; }
    public string BoardId { get; set; }
    public int? ProjectRepositoryId { get; set; }
    public int? SpecId { get; set; }
    public int FailureCount { get; set; }
    public bool NeedsManualIntervention { get; set; }
    public DateTime? InProgressStartedAt { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime UpdatedAt { get; set; }
    public bool IsManualCreated { get; set; }
    public List<PhaseProgressResponse> PhaseProgressList { get; set; }
    public List<FileChangeResponse> FileChangeList { get; set; }
    public ProjectRepositoryResponse ProjectRepository { get; set; }
}

/// <summary>
/// 阶段进度响应结构
/// </summary>
public class PhaseProgressResponse
{
    public int Id { get; set; }
    public int ExecutionCardId { get; set; }
    public int Phase { get; set; }
    public int Status { get; set; }
    public DateTime? StartedAt { get; set; }
    public DateTime? CompletedAt { get; set; }
    public string PhaseLog { get; set; }
}

/// <summary>
/// 文件变更响应结构
/// </summary>
public class FileChangeResponse
{
    public int Id { get; set; }
    public int ExecutionCardId { get; set; }
    public string FilePath { get; set; }
    public int ChangeType { get; set; }
    public string CommitHash { get; set; }
    public DateTime ChangedAt { get; set; }
}

/// <summary>
/// 项目仓库响应结构
/// </summary>
public class ProjectRepositoryResponse
{
    public int Id { get; set; }
    public string Name { get; set; }
    public string LocalWorkingDir { get; set; }
    public string GitRemoteUrl { get; set; }
    public string DefaultBranch { get; set; }
    public string Description { get; set; }
}
```

### 6. 验证其他 API 接口

确保以下 API 接口已在后端实现：

- `GET /api/ekanban/ExecutionCard/GetKanbanData` - 已实现
- `POST /api/ekanban/AzureBoardsSync/TriggerSync` - 已实现
- `POST /api/ekanban/AiExecution/TriggerExecution` - 已实现
- `POST /api/ekanban/ExecutionCard/TriggerReExecute` - 已实现

## 数据结构设计

### 1. 卡片详情返回结构

```json
{
  "code": 200,
  "data": {
    "id": 1,
    "title": "卡片标题",
    "description": "卡片描述",
    "status": 0,
    "executorType": 1,
    "boardWorkItemId": 123,
    "boardId": "456",
    "projectRepositoryId": 1,
    "specId": 2,
    "failureCount": 0,
    "needsManualIntervention": false,
    "inProgressStartedAt": "2026-04-07T10:00:00",
    "createdAt": "2026-04-07T09:00:00",
    "updatedAt": "2026-04-07T09:30:00",
    "isManualCreated": false,
    "phaseProgressList": [
      {
        "id": 1,
        "executionCardId": 1,
        "phase": 1,
        "status": 2,
        "startedAt": "2026-04-07T09:10:00",
        "completedAt": "2026-04-07T09:20:00",
        "phaseLog": "阶段执行日志"
      }
    ],
    "fileChangeList": [
      {
        "id": 1,
        "executionCardId": 1,
        "filePath": "src/test.ts",
        "changeType": 1,
        "commitHash": "abc123",
        "changedAt": "2026-04-07T09:15:00"
      }
    ],
    "projectRepository": {
      "id": 1,
      "name": "项目名称",
      "localWorkingDir": "D:/projects/test",
      "gitRemoteUrl": "https://github.com/test/test.git",
      "defaultBranch": "main",
      "description": "项目描述"
    }
  }
}
```

## 实施步骤

1. **添加返回结构类**：创建统一的返回结构类和卡片详情响应结构类
2. **更新服务层**：在 `IExecutionCardService` 接口中添加 `GetDetailAsync` 方法，并在 `ExecutionCardService` 中实现
3. **更新控制器**：在 `ExecutionCardController` 中添加 `GetDetail` 方法，并添加必要的依赖注入
4. **配置 JSON 序列化**：确保 JSON 序列化时将 PascalCase 转换为 camelCase
5. **测试 API 接口**：测试所有前端调用的 API 接口是否正常工作
6. **验证前端功能**：验证前端功能是否能正常与后端交互

## 注意事项

- 确保 `ITaskPhaseProgressRepository`、`ITaskFileChangeRepository` 和 `IProjectRepository` 已在系统中实现
- 确保返回的数据结构与前端期望的结构一致，包括字段名称（使用 camelCase）和数据类型
- 处理可能的异常情况，确保 API 接口的稳定性
- 测试所有前端调用的 API 接口，确保它们都能正常工作
- 遵循三层结构，保持代码的清晰性和可维护性
- 复用请求参数和返回参数，减少代码冗余