# 手动增加看板功能 - 后端实现方案

## 一、项目架构分析

通过分析现有项目结构，我们了解到：

1. **架构模式**：采用三层架构（控制器层、服务层、仓库层）
2. **依赖注入**：使用依赖注入模式，控制器注入服务和仓库
3. **数据模型**：ExecutionCard模型在 `src/modules/EKanban/Models/ExecutionCard.cs` 中定义
4. **控制器**：ExecutionCardController在 `src/backend/VOL.WebApi/Controllers/EKanban/ExecutionCardController.cs` 中实现
5. **服务层**：业务逻辑在服务层实现，如 `IExecutionCardService`
6. **仓库层**：数据访问在仓库层实现，如 `IExecutionCardRepository`

## 二、数据模型修改

### 1. ExecutionCard模型修改

**文件**: `src/modules/EKanban/Models/ExecutionCard.cs`

在ExecutionCard类中添加IsManualCreated字段：

```csharp
/// <summary>
/// 是否手动创建（true: 手动创建, false: Azure同步）
/// </summary>
public bool IsManualCreated { get; set; } = false;
```

**注意**：ProjectRepositoryId和SpecId字段在现有模型中已经存在，无需重复添加。

### 2. 数据库迁移

**数据库迁移脚本**（SQL Server）：

```sql
-- 添加IsManualCreated字段
ALTER TABLE ExecutionCards
ADD IsManualCreated BIT NOT NULL DEFAULT 0;

-- 创建索引（可选，提高查询性能）
CREATE INDEX IX_ExecutionCards_IsManualCreated
ON ExecutionCards(IsManualCreated);
```

## 三、服务层实现

### 1. IExecutionCardService接口修改

**文件**: `src/modules/EKanban/IServices/IExecutionCardService.cs`

添加手动看板相关方法：

```csharp
/// <summary>
/// 创建手动看板卡片
/// </summary>
/// <param name="request">创建请求</param>
/// <returns>创建的卡片</returns>
Task<ExecutionCard> CreateManualCardAsync(ManualCardCreateRequest request);

/// <summary>
/// 更新手动看板卡片
/// </summary>
/// <param name="request">更新请求</param>
/// <returns>更新后的卡片</returns>
Task<ExecutionCard> UpdateManualCardAsync(ManualCardUpdateRequest request);

/// <summary>
/// 删除手动看板卡片
/// </summary>
/// <param name="id">卡片ID</param>
/// <returns>是否删除成功</returns>
Task<bool> DeleteManualCardAsync(int id);

/// <summary>
/// 获取手动看板卡片列表
/// </summary>
/// <param name="page">页码</param>
/// <param name="pageSize">每页大小</param>
/// <param name="search">搜索关键词</param>
/// <param name="status">状态筛选</param>
/// <returns>卡片列表和总数</returns>
Task<(List<ExecutionCard> Items, int Total)> GetManualCardsAsync(int page, int pageSize, string? search, int? status);
```

### 2. ExecutionCardService实现

**文件**: `src/modules/EKanban/Services/ExecutionCardService.cs`

添加手动看板相关方法的实现：

```csharp
/// <summary>
/// 创建手动看板卡片
/// </summary>
/// <param name="request">创建请求</param>
/// <returns>创建的卡片</returns>
public async Task<ExecutionCard> CreateManualCardAsync(ManualCardCreateRequest request)
{
    if (string.IsNullOrWhiteSpace(request.Title))
    {
        throw new ArgumentException("标题不能为空");
    }
    
    if (request.Title.Length > 500)
    {
        throw new ArgumentException("标题长度不能超过500个字符");
    }
    
    var card = new ExecutionCard
    {
        Title = request.Title,
        Description = request.Description,
        Status = ExecutionCardStatus.New,
        ExecutorType = ExecutorType.Human,
        BoardWorkItemId = 0, // 手动创建的卡片没有Azure WorkItem ID
        BoardId = request.BoardId ?? "Manual", // 手动创建的卡片使用指定或默认BoardId
        IsManualCreated = true,
        ProjectRepositoryId = request.ProjectRepositoryId,
        SpecId = request.SpecId,
        CreatedAt = DateTime.UtcNow,
        LastUpdated = DateTime.UtcNow
    };
    
    await _repository.AddAsync(card);
    return card;
}

/// <summary>
/// 更新手动看板卡片
/// </summary>
/// <param name="request">更新请求</param>
/// <returns>更新后的卡片</returns>
public async Task<ExecutionCard> UpdateManualCardAsync(ManualCardUpdateRequest request)
{
    if (string.IsNullOrWhiteSpace(request.Title))
    {
        throw new ArgumentException("标题不能为空");
    }
    
    if (request.Title.Length > 500)
    {
        throw new ArgumentException("标题长度不能超过500个字符");
    }
    
    var card = await _repository.FindByIdAsync(request.Id);
    if (card == null)
    {
        throw new ArgumentException("卡片不存在");
    }
    
    if (!card.IsManualCreated)
    {
        throw new ArgumentException("只能编辑手动创建的卡片");
    }
    
    card.Title = request.Title;
    card.Description = request.Description;
    card.BoardId = request.BoardId ?? card.BoardId;
    card.ProjectRepositoryId = request.ProjectRepositoryId;
    card.SpecId = request.SpecId;
    card.LastUpdated = DateTime.UtcNow;
    
    await _repository.UpdateAsync(card);
    return card;
}

/// <summary>
/// 删除手动看板卡片
/// </summary>
/// <param name="id">卡片ID</param>
/// <returns>是否删除成功</returns>
public async Task<bool> DeleteManualCardAsync(int id)
{
    var card = await _repository.FindByIdAsync(id);
    if (card == null)
    {
        throw new ArgumentException("卡片不存在");
    }
    
    if (!card.IsManualCreated)
    {
        throw new ArgumentException("只能删除手动创建的卡片");
    }
    
    await _repository.DeleteAsync(card);
    return true;
}

/// <summary>
/// 获取手动看板卡片列表
/// </summary>
/// <param name="page">页码</param>
/// <param name="pageSize">每页大小</param>
/// <param name="search">搜索关键词</param>
/// <param name="status">状态筛选</param>
/// <returns>卡片列表和总数</returns>
public async Task<(List<ExecutionCard> Items, int Total)> GetManualCardsAsync(int page, int pageSize, string? search, int? status)
{
    var query = _repository.Query().Where(c => c.IsManualCreated);
    
    // 搜索筛选
    if (!string.IsNullOrWhiteSpace(search))
    {
        query = query.Where(c => c.Title.Contains(search));
    }
    
    // 状态筛选
    if (status.HasValue)
    {
        query = query.Where(c => c.Status == (ExecutionCardStatus)status.Value);
    }
    
    // 计算总数
    var total = await query.CountAsync();
    
    // 分页
    var cards = await query
        .OrderByDescending(c => c.CreatedAt)
        .Skip((page - 1) * pageSize)
        .Take(pageSize)
        .ToListAsync();
    
    return (cards, total);
}
```

### 3. 请求模型定义

创建请求模型类：

```csharp
/// <summary>
/// 手动看板创建请求
/// </summary>
public class ManualCardCreateRequest
{
    public string Title { get; set; } = string.Empty;
    public string? Description { get; set; }
    public string? BoardId { get; set; }
    public int? ProjectRepositoryId { get; set; }
    public int? SpecId { get; set; }
}

/// <summary>
/// 手动看板更新请求
/// </summary>
public class ManualCardUpdateRequest
{
    public int Id { get; set; }
    public string Title { get; set; } = string.Empty;
    public string? Description { get; set; }
    public string? BoardId { get; set; }
    public int? ProjectRepositoryId { get; set; }
    public int? SpecId { get; set; }
}
```

## 四、控制器实现

### 1. ExecutionCardController修改

**文件**: `src/backend/VOL.WebApi/Controllers/EKanban/ExecutionCardController.cs`

添加手动看板相关接口：

```csharp
/// <summary>
/// 手动创建看板卡片
/// </summary>
[HttpPost]
public async Task<IActionResult> CreateManualCard([FromBody] ManualCardCreateRequest request)
{
    try
    {
        var card = await _service.CreateManualCardAsync(request);
        return Ok(card);
    }
    catch (ArgumentException ex)
    {
        return BadRequest(new { message = ex.Message });
    }
    catch (Exception ex)
    {
        return StatusCode(500, new { message = "创建失败: " + ex.Message });
    }
}

/// <summary>
/// 编辑手动看板卡片
/// </summary>
[HttpPost]
public async Task<IActionResult> UpdateManualCard([FromBody] ManualCardUpdateRequest request)
{
    try
    {
        var card = await _service.UpdateManualCardAsync(request);
        return Ok(card);
    }
    catch (ArgumentException ex)
    {
        return BadRequest(new { message = ex.Message });
    }
    catch (Exception ex)
    {
        return StatusCode(500, new { message = "更新失败: " + ex.Message });
    }
}

/// <summary>
/// 删除手动看板卡片
/// </summary>
[HttpPost]
public async Task<IActionResult> DeleteManualCard(int id)
{
    try
    {
        await _service.DeleteManualCardAsync(id);
        return Ok(new { message = "删除成功" });
    }
    catch (ArgumentException ex)
    {
        return BadRequest(new { message = ex.Message });
    }
    catch (Exception ex)
    {
        return StatusCode(500, new { message = "删除失败: " + ex.Message });
    }
}

/// <summary>
/// 获取所有手动创建的看板卡片（支持分页和筛选）
/// </summary>
[HttpGet]
public async Task<IActionResult> GetManualCards(
    [FromQuery] int page = 1,
    [FromQuery] int pageSize = 10,
    [FromQuery] string? search = null,
    [FromQuery] int? status = null)
{
    try
    {
        var (items, total) = await _service.GetManualCardsAsync(page, pageSize, search, status);
        return Ok(new { total, items });
    }
    catch (Exception ex)
    {
        return StatusCode(500, new { message = "获取卡片列表失败: " + ex.Message });
    }
}
```

## 五、仓库层实现

### 1. IExecutionCardRepository接口修改

**文件**: `src/modules/EKanban/IRepositories/IExecutionCardRepository.cs`

确保仓库接口包含基本的CRUD方法：

```csharp
/// <summary>
/// 添加卡片
/// </summary>
/// <param name="card">卡片实体</param>
/// <returns></returns>
Task AddAsync(ExecutionCard card);

/// <summary>
/// 更新卡片
/// </summary>
/// <param name="card">卡片实体</param>
/// <returns></returns>
Task UpdateAsync(ExecutionCard card);

/// <summary>
/// 删除卡片
/// </summary>
/// <param name="card">卡片实体</param>
/// <returns></returns>
Task DeleteAsync(ExecutionCard card);

/// <summary>
/// 根据ID查找卡片
/// </summary>
/// <param name="id">卡片ID</param>
/// <returns>卡片实体</returns>
Task<ExecutionCard?> FindByIdAsync(int id);

/// <summary>
/// 查询卡片
/// </summary>
/// <returns>查询对象</returns>
ISugarQueryable<ExecutionCard> Query();
```

### 2. ExecutionCardRepository实现

**文件**: `src/modules/EKanban/Repositories/ExecutionCardRepository.cs`

确保仓库实现包含相应的方法：

```csharp
/// <summary>
/// 添加卡片
/// </summary>
/// <param name="card">卡片实体</param>
/// <returns></returns>
public async Task AddAsync(ExecutionCard card)
{
    await _dbContext.Db.Insertable(card).ExecuteCommandAsync();
}

/// <summary>
/// 更新卡片
/// </summary>
/// <param name="card">卡片实体</param>
/// <returns></returns>
public async Task UpdateAsync(ExecutionCard card)
{
    await _dbContext.Db.Updateable(card).ExecuteCommandAsync();
}

/// <summary>
/// 删除卡片
/// </summary>
/// <param name="card">卡片实体</param>
/// <returns></returns>
public async Task DeleteAsync(ExecutionCard card)
{
    await _dbContext.Db.Deleteable(card).ExecuteCommandAsync();
}

/// <summary>
/// 根据ID查找卡片
/// </summary>
/// <param name="id">卡片ID</param>
/// <returns>卡片实体</returns>
public async Task<ExecutionCard?> FindByIdAsync(int id)
{
    return await _dbContext.Db.Queryable<ExecutionCard>().FirstAsync(x => x.Id == id);
}

/// <summary>
/// 查询卡片
/// </summary>
/// <returns>查询对象</returns>
public ISugarQueryable<ExecutionCard> Query()
{
    return _dbContext.Db.Queryable<ExecutionCard>();
}
```

## 六、实现注意事项

1. **架构一致性**：
   - 遵循项目现有的三层架构模式
   - 保持与现有代码风格和命名约定的一致性
   - 使用依赖注入模式，确保代码的可测试性和可维护性

2. **数据验证**：
   - 在服务层实现数据验证，确保输入数据的合法性
   - 控制器层捕获并处理验证异常，返回清晰的错误信息

3. **错误处理**：
   - 实现统一的错误处理机制
   - 区分业务逻辑错误和系统错误
   - 返回结构化的错误信息，便于前端处理

4. **性能优化**：
   - 添加适当的数据库索引，优化查询性能
   - 使用分页查询，减少数据传输量
   - 合理使用缓存，提高系统响应速度

5. **安全性**：
   - 考虑添加适当的权限控制，确保只有授权用户可以操作手动看板
   - 对输入数据进行严格验证，防止恶意输入
   - 添加操作审计日志，便于问题排查和追踪

6. **测试覆盖**：
   - 为新增的服务层方法添加单元测试
   - 测试手动看板的创建、编辑、删除和查询功能
   - 验证与现有Azure同步功能的兼容性

通过以上实现，我们可以确保手动看板功能与现有系统无缝集成，同时保持代码的一致性和可维护性。